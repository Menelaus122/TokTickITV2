# Lab 2 — Peer Review Record

**Author:** Poomipat Apiwattanaphong — 67070501035 — GitHub: [@Menelaus122](https://github.com/Menelaus122)<br>**Peer reviewer:** Wirachat — 67070501041 — GitHub: [@WirachatTH](https://github.com/WirachatTH)
<br>**Repository:** [Menelaus122/TokTickITV2](https://github.com/Menelaus122/TokTickITV2) · **Integration branch:** `lab2-staging`

## Pull Requests I authored (reviewed by my partner)

| PR | Branch | Reviewer verdict |
| :--- | :--- | :--- |
| [#20](https://github.com/Menelaus122/TokTickITV2/pull/20) | feature/1-sprint-specification | Approved |
| [#21](https://github.com/Menelaus122/TokTickITV2/pull/21) | feature/2-database-and-seed | Approved |
| [#22](https://github.com/Menelaus122/TokTickITV2/pull/22) | feature/3-ui-foundation | Approved |
| [#23](https://github.com/Menelaus122/TokTickITV2/pull/23) | feature/4-requester-context | Approved |
| [#24](https://github.com/Menelaus122/TokTickITV2/pull/24) | feature/5-ticket-creation | Approved |
| [#25](https://github.com/Menelaus122/TokTickITV2/pull/25) | feature/6-my-tickets | Approved |
| [#26](https://github.com/Menelaus122/TokTickITV2/pull/26) | feature/7-ticket-detail-attachments | Approved |
| [#27](https://github.com/Menelaus122/TokTickITV2/pull/27) | feature/8-reference-data-and-shell | Approved |
| [#28](https://github.com/Menelaus122/TokTickITV2/pull/28) | feature/9-automated-tests-and-screenshots | Approved |
| [#30](https://github.com/Menelaus122/TokTickITV2/pull/30) | feature/10-create-ticket-completion | Approved |

All ten Pull Requests were approved **and merged** by [@WirachatTH](https://github.com/WirachatTH), never by me, per the Part 9 agreement in the workflow guide. Each PR is linked to its GitHub Issue through the Development panel, and every feature branch reached `lab2-staging` through a Pull Request — nothing was pushed directly to `lab2-staging` or `main`.

| Feature | **Reviewer comment I received:** | **How I responded:** |
| :--- | :--- | :--- |
| feature1 | I have read all document files. They are very detailed and they cover everything of your first issue perfectly. You may start the second issue right away!<br><br>*Wirachat 67070501041* | Thx so much kub. I will hurry moving forward na kub |
| feature2 | The implementation meets all acceptance criteria without any mistakes or unnecessary features.<br><br>• **Schema Design:** The single `removedAt` timestamp effectively prevents state drift, and the migration safely extends the existing tables.<br>• **Seed Strategy:** The upsert logic, specifically including the update branch, guarantees true idempotency by correcting manual data drift.<br>• **Database Constraints:** The chosen indexes efficiently target the required queries, and the explicit string bounds enforce business logic directly at the database layer.<br><br>Ready to merge na ja!<br><br>*Wirachat 67070501041* | Thx for your review and merging kub. I will start work on Issue3 and will let u know if it is finished. |
| feature3 | Excellent work on the UI Foundation. I pulled down your branch and verified everything against the acceptance criteria:<br><br>• The Zen Green Theme tokens are perfectly isolated in `:root` with zero stray hex codes.<br>• The separation of components (form controls, badges, cards, buttons) is spot on, keeping everything perfectly in scope without adding premature screens.<br>• Validation, read-only states, and accessibility attributes behave exactly as specified.<br>• The mobile breakpoints and responsive behaviors are fully functional.<br><br>Tested locally and all 60 tests passed flawlessly. I'll merge and let you continue working on the next issue right away.<br><br>*Wirachat 67070501041* | Thx for ur review again. I will start head toward Issue4 kub. |
| feature4 | Approved:<br><br>• Pulled the branch and ran it myself: frontend suite is 6 files / 91 tests, all green, `tsc --noEmit` clean — matches what you posted exactly.<br>• Read through `RequesterContext`, `RequesterSelection`, and `AppShell` line by line. The `available: undefined` vs `available: []` distinction in the provider is a genuinely subtle bug, and the fix is correct — good catch, and good that there's a regression test pinning it down.<br>• `/api/requesters` is properly scoped (active-only, no credential fields in the payload) and `App.tsx` / no-`react-router` calls are exactly as described — nothing sneaking in ahead of scope.<br>• Only thing I couldn't do myself was re-run the backend Supertest suite live (sandbox couldn't reach the Prisma engine download), so I'm taking the 8/8 on trust from the code — it lines up with what `requesters.api.test.ts` actually asserts, so no concerns there.<br><br>You are ready to go to proceed on the next issue!<br><br>*Wirachat 67070501041* | Thx so much for ur review, I will start on Issue5. |
| feature5 | I have thoroughly ran all tests mentioned in the markdown, and I can confirm that the results are very valid! You may now proceed with the next issue krub!<br><br>*Wirachat 67070501041* | Thx kub. We are half way leaw. Let's head on toward endpoint. |
| feature6 | Awesome work.<br><br>A few highlights from my review:<br>• **Security & Data Isolation:** Enforcing the `requesterId` check natively inside the Prisma `where` clause is perfectly implemented. There is zero risk of cross-requester data leakage.<br>• **Robust Query Parsing:** Love that you're explicitly enforcing strict `400` errors for invalid pagination/sorting inputs rather than silently defaulting them. It prevents masked client bugs.<br>• **Flawless Test Coverage:** 132 frontend tests and 102 backend tests is fantastic. Huge kudos for catching and proactively fixing the DB concurrency test flake (`fileParallelism: false`). That saves us a massive headache down the line!<br><br>You are now good to start the next issue right away.<br><br>*Wirachat 67070501041* | ok kub, I will start the next issue and will finished it asap |
| feature7 | I've tested your submission against a clean database and verified both the client and server tests:<br><br>• Client: All 176 tests pass (10/10 files) + clean `tsc --noEmit`.<br>• Server: All 160 tests pass (11/11 files) against PostgreSQL.<br><br>All passed without fails! You are good to proceed on the next issue.<br><br>*Wirachat 67070501041* | Thx for ur review again kub. I will start the next issue asap. |
| feature8 | Everything has been verified:<br><br>• Client: All 202 tests pass (11/11 files), including all 26 new tests in `Navigation.test.tsx`.<br>• Server: All 160 tests pass (11/11 files).<br>• Build & Type Check: Clean `tsc --noEmit` and production build with 0 stray `.js` files.<br><br>Looking great so far krub. You can begin the next issue right away! | Thx kub. There are only 2 issue left. I will keep going forward kub. |
| feature9 | Pulled your zip, spun up the full stack in Docker, and ran everything myself rather than just reading the description — backend 161/161, frontend 202/202, Playwright 19/19, all matching your numbers exactly. `tsc --noEmit` clean on both sides too.<br><br>Went and read the actual diffs behind your two bug writeups — the `window.open()` → blob-fetch fix in `api.ts` and the `state.from` → explicit `ROUTES.list` fix in `AppShell.tsx` both check out exactly as described, not just plausible-sounding. Same for the pagination test rewrite — the ownership-count assertion is there, plus the separate `totalItems` check.<br><br>Respect for flagging the FR-15 gap yourself instead of letting it slide since it's "just" a testing PR — checked `CreateTicket.tsx` and confirmed there's genuinely no attachment handling there yet. That kind of honest scope note is worth more than a clean-looking PR that hides it.<br><br>All good to go! Good luck with the few remaining issues na krub<br><br>*Wirachat 67070501041* | Thank you for your review kub. I will start on issue10 |
| feature10 | Very good! I pulled it, ran the full stack myself instead of trusting the numbers. Backend 161/161 (unchanged, as expected), frontend 220/220, Playwright 21/21, `tsc --noEmit` clean both sides. All match your description exactly.<br><br>Went and read the actual submit handler rather than taking "doesn't auto-navigate anymore" on faith — the comment and the code both back it up, ticket creation and the upload loop are cleanly separated, and a failed file genuinely doesn't touch the created ticket, just gets collected into the per-file failure list. Confirmed the whole thing live in a browser too: submitted a ticket, stayed on the form, saw the real confirmation with the ticket number, and View ticket actually lands on Ticket Detail. (My first pass at that check gave two false fails — I'd missed filling Requested Priority and had the wrong ticket-number format in my regex. Both on me, not your code; fixed the script and it's clean.)<br><br>You are very close to finishing this phase of the project. Good luck proceeding!<br><br>*Wirachat 67070501041* | Thx, for ur review kub. I will start on issue11, which is about documentation. |

## Pull Requests I reviewed for my partner

Partner repository: [WirachatTH/toktickit](https://github.com/WirachatTH/toktickit) · integration branch `lab2-staging`

| Me | PR | Branch | Reviewer verdict |
| :--- | :--- | :--- | :--- |
| 67070501035 | [#29](https://github.com/WirachatTH/toktickit/pull/29) | feature/1-doc-prep | Approved |
| 67070501035 | [#30](https://github.com/WirachatTH/toktickit/pull/30) | feature/2-data-model | Changes requested → Approved |
| 67070501035 | [#31](https://github.com/WirachatTH/toktickit/pull/31) | feature/3-design-system-shell | Approved |
| 67070501035 | [#32](https://github.com/WirachatTH/toktickit/pull/32) | feature/4-requester-selector | Changes requested → Approved |
| 67070501035 | [#33](https://github.com/WirachatTH/toktickit/pull/33) | feature/5-create-ticket | Approved |
| 67070501035 | [#34](https://github.com/WirachatTH/toktickit/pull/34) | feature/6-attachment-lifecycle | Changes requested → Approved |
| 67070501035 | [#35](https://github.com/WirachatTH/toktickit/pull/35) | feature/7-my-tickets | Changes requested → Approved |
| 67070501035 | [#36](https://github.com/WirachatTH/toktickit/pull/36) | feature/8-ticket-detail | Approved |
| 67070501035 | [#37](https://github.com/WirachatTH/toktickit/pull/37) | feature/9-responsive-e2e-qa | Changes requested → Approved |

I merged all nine after approving, as the author should not merge their own Pull Request. Five of the nine needed a round of **Changes requested** first; each was re-reviewed and approved once the fixes landed on the same branch.

| Feature | **My comment:** | **Partner's response:** |
| :--- | :--- | :--- |
| feature1 | I have review your work all pass the acceptance criteria kub. | Thanks for reviewing. I will proceed on the next issue right away. |
| feature2 | **Changes requested** — all 3 acceptance criteria pass, one defect to fix first.<br><br>Verified against a database built from committed migrations only, in an isolated container. I checked §7 field-by-field against the live database rather than reading the diff, and it matches — every field, type, nullability, and default, all ten indexes and constraints, and the delete rules (three `RESTRICT` on Ticket's parents, `CASCADE` on Attachment). Good call leaving `Category` alone rather than bolting on an `isActive` "for consistency". Seed ran three times with identical counts.<br><br>🔴 **To fix — the test suite cannot be run twice.** The restrict-on-delete test creates a `Temp Category` and asserts the delete is blocked. That assertion passing is exactly what makes the row impossible to clean up, so it stays behind and rows pile up on every run.<br><br>**Re-review: Approved.** Audited the codebase against `specification.md §7` again and the defect is genuinely fixed. | Got it. I'll report back as soon as it's fixed.<br><br>Thank you very much for taking your time into reviewing my progress! I'll notify as soon as I finish the next issue kub. |
| feature3 | **Approved.** Checked out `feature/3-design-system-shell`, audited the design system components, CSS tokens, and accessibility wiring, and executed the full test suite. | Thank you for reviewing in full details! I will proceed with the next issue right away and report back asap. |
| feature4 | **Changes requested.** The implementation is solid and both acceptance criteria hold functionally — I verified that directly, not by reading the diff. But neither AC is protected by a test that can actually fail: in both cases I deleted the production code satisfying the criterion and watched the suite stay green. That is the only reason I am not approving. All verification ran from a clean `git archive` export with a fresh `npm ci` against a throwaway Postgres, not a dev database that might already hold the right rows.<br><br>🔴 **Blocking 1 — AC-02 is not covered against the real route.** I removed `<RequireRequester>` from all three guarded routes and the suite still passed.<br>🔴 **Blocking 2 — the `/api/systems` active-only filter has no test.** I deleted `where: { isActive: true }` and nothing failed.<br><br>**Re-review: Approved** at `31bbd82`. | Got it. I'll fix the issues as soon as possible.<br><br>Thank you for your review! I will now proceed on the next issue. |
| feature5 | **Approve, with three small follow-ups.** Every acceptance criterion in scope genuinely passes — AC-01, AC-04, AC-05, AC-06, AC-07 and AC-15 — and I confirmed the *behaviour*, not just that the suite was green. All verification ran from a clean `git archive` export with a fresh `npm ci`. | Thank you for taking your time to review the progress! I'll start working on the next issue right away! |
| feature6 | **Changes requested — three blockers.** Verified 71/71 server and 66/66 client from a pristine export, `tsc --noEmit` clean, and zero drift across three consecutive runs (`tickets=0 attachments=0 filesOnDisk=0 orphanFiles=0`). Mutation testing showed 9 of 11 rules load-bearing.<br><br>🔴 **Blocking 1 —** out-of-`Int32` ids return `500` on all four new routes.<br>🔴 **Blocking 2 —** the add route dropped the create route's Multer limits.<br>🔴 **Blocking 3 —** concurrent soft-remove race.<br><br>**Re-review: Approve.** All three blockers are genuinely fixed and the acceptance criteria hold. | I have checked and fixed the problems found in this issue na krub.<br><br>Thank you very much for your effort in reviewing my progress! I'll proceed on the next issue right away! |
| feature7 | **Changes requested.** Two of #25's four acceptance criteria do not pass.<br><br>🔴 **Blocking 1 —** `categoryId` / `relatedSystemId` outside `Int32` range return `500` instead of a client error.<br>🔴 **Blocking 2 —** AC-4 has no screenshots.<br><br>Non-blocking: RESP-01 was marked Pass without evidence, and the `id: "desc"` tiebreaker needed a note.<br><br>**Re-review: Approve.** All four acceptance criteria now pass. | I have fixed the problems you've found. Let me know if there's still anything missing!<br><br>Thank you very much for your kind help! |
| feature8 | **Approve, with four follow-ups.** All five of #26's acceptance criteria pass, and every AC-critical behaviour is mutation-protected on both sides. One follow-up is a genuine regression in test hygiene; the other three are accuracy and evidence. Verified from a clean `git archive` export, fresh `npm ci`, and a throwaway Postgres. | Thank you mak mak krub. |
| feature9 | **Changes requested — one blocker, one documentation bug.** Six of #27's acceptance criteria pass.<br><br>🔴 **Blocker —** the E2E screenshots never leave the container, so AC-3 has no artifacts on the host.<br>⚠️ **Documentation bug —** the documented run command finds no tests.<br><br>The three E2E bugs found are real and the reasoning behind the fixes is good, and the checklist is honest, which is the point of it.<br><br>**Re-review: Approve.** Both findings are fixed, and I verified the blocker myself rather than taking it on trust. | Got it. I will fix it asap!<br><br>Thank you for taking your time to review my progress! |
